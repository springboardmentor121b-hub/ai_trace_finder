# Auto detect text files and perform LF normalization
* text=auto

# Machine learning model binary files
*.pkl filter=lfs diff=lfs merge=lfs -text
*.npy filter=lfs diff=lfs merge=lfs -text
*.pdf binary
*.png binary
